# WordPress API base URL (required for live content and site-config)
VITE_WP_API_URL=https://wpcms.ccsprocert.com/wp-json

# Content source selector (phase 1 default = REST)
# Supported: rest | wordpress_graphql
VITE_CONTENT_SOURCE=rest

# Phase 2 placeholder for WPGraphQL endpoint
# Example: https://wpcms.ccsprocert.com/graphql
# VITE_WP_GRAPHQL_ENDPOINT=

# Optional: build-time fallback; "true" = show Coming Soon if site-config not yet loaded
# VITE_COMING_SOON=

# Optional: Set to enable password gate during development. Remove to disable.
# VITE_DEV_PASSWORD=
