# WordPress headless CMS API base URL (no trailing slash).
# Used by the frontend to fetch landing page content.
# Example: https://wpcms.ccsprocert.com/wp-json
VITE_WP_API_URL=https://wpcms.ccsprocert.com/wp-json

# Set to "true" to show a "Coming Soon" page instead of the full site.
# Omit or set to anything else to run the site normally.
# VITE_COMING_SOON=true
